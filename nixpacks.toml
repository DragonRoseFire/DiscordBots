[phases.setup]
nixPkgs = ["nodejs_24"]

[phases.install]
cmds = ["npm install -g pnpm && pnpm install"]

[phases.build]
cmds = ["pnpm --filter @workspace/db run push || true"]

[start]
cmd = "pnpm --filter @workspace/discord-bot run start"
